import React from 'react';

// This component is intentionally disabled to allow open access via pre-configured API key.
const ApiKeySelector: React.FC<any> = () => {
  return null;
};

export default ApiKeySelector;